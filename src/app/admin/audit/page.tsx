import { getCurrentUser } from "@/features/auth/session";import { db } from "@/lib/db";export default async function Audit(){const u=(await getCurrentUser())!;const rows=await db.auditLog.findMany({where:{organisationId:u.employee!.organisationId},include:{actor:{select:{email:true}}},orderBy:{createdAt:"desc"},take:200});return <main className="page"><h1>Audit log</h1><p className="muted">Audit records are append-only and cannot be edited here.</p><div className="table-wrap"><table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.createdAt.toLocaleString("en-GB")}</td><td>{r.actor?.email??"System"}</td><td>{r.action.replaceAll("_"," ")}</td><td>{r.entityType} · {r.entityId}</td></tr>)}</tbody></table></div></main>}
