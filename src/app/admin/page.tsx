/* eslint-disable @next/next/no-html-link-for-pages */
import { getCurrentUser } from "@/features/auth/session";import { db } from "@/lib/db";
export default async function Admin(){const user=(await getCurrentUser())!;const org=user.employee!.organisationId;const [employees,pending,month,low]=await Promise.all([db.employee.count({where:{organisationId:org,active:true}}),db.leaveRequest.count({where:{employee:{organisationId:org},status:"PENDING"}}),db.leaveRequest.count({where:{employee:{organisationId:org},createdAt:{gte:new Date(new Date().getFullYear(),new Date().getMonth(),1)}}}),db.leaveEntitlement.count({where:{employee:{organisationId:org},entitlementHours:{lt:40}}})]);return <main className="page"><p className="eyebrow">Administration</p><h1>Admin dashboard</h1><section className="card-grid"><article className="card"><h2>Active employees</h2><p className="metric">{employees}</p></article><article className="card"><h2>Pending requests</h2><p className="metric">{pending}</p></article><article className="card"><h2>Requests this month</h2><p className="metric">{month}</p></article><article className="card"><h2>Low configured entitlement</h2><p className="metric">{low}</p></article></section><h2>Quick actions</h2><div className="card nav"><a href="/admin/employees">Manage employees</a><a href="/admin/locations">Locations</a><a href="/admin/departments">Departments</a><a href="/admin/bank-holidays">Bank holidays</a><a href="/admin/reports">Reports</a><a href="/admin/audit">Audit log</a></div></main>}
