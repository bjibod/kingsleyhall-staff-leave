import { getCurrentUser } from "@/features/auth/session";import { db } from "@/lib/db";export default async function Locations(){const u=(await getCurrentUser())!;const rows=await db.location.findMany({where:{organisationId:u.employee!.organisationId},orderBy:{name:"asc"}});return <main className="page"><h1>Locations</h1><div className="table-wrap"><table><thead><tr><th>Name</th><th>Address</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.address??"—"}</td><td><span className="badge">{r.active?"ACTIVE":"DISABLED"}</span></td></tr>)}</tbody></table></div></main>}
