import { notFound } from "next/navigation";import { db } from "@/lib/db";
export default async function EmployeeDetail({params}:{params:Promise<{id:string}>}){const employee=await db.employee.findUnique({where:{id:(await params).id},include:{department:true,location:true,manager:true,workingPatterns:true,entitlements:{include:{leaveYear:true}}}});if(!employee)notFound();return <main className="page"><p className="eyebrow">Administration / Employees</p><h1>{employee.firstName} {employee.lastName}</h1><section className="card-grid"><article className="card"><h2>Employment</h2><p>{employee.jobTitle}</p><p>{employee.department?.name} · {employee.location?.name}</p><p>{employee.employmentType.replaceAll("_"," ")}</p></article><article className="card"><h2>Manager</h2><p>{employee.manager?`${employee.manager.firstName} ${employee.manager.lastName}`:"Not assigned"}</p></article><article className="card"><h2>Entitlement</h2>{employee.entitlements.map(e=><p key={e.id}>{e.leaveYear.name}: {Number(e.entitlementHours)+Number(e.carriedForwardHours)+Number(e.manualAdjustmentHours)}h</p>)}</article></section></main>}
