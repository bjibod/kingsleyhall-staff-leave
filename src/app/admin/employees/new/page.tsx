import { getCurrentUser } from "@/features/auth/session";
import { EmployeeOnboardingForm } from "@/components/employee-onboarding-form";
import { db } from "@/lib/db";
export default async function NewEmployee(){const user=(await getCurrentUser())!;const organisationId=user.employee!.organisationId;const [locations,departments,managers]=await Promise.all([db.location.findMany({where:{organisationId,active:true},select:{id:true,name:true},orderBy:{name:"asc"}}),db.department.findMany({where:{organisationId,active:true},select:{id:true,name:true},orderBy:{name:"asc"}}),db.employee.findMany({where:{organisationId,active:true},select:{id:true,firstName:true,lastName:true,employeeNumber:true},orderBy:{lastName:"asc"}})]);return <main className="page"><p className="eyebrow">Administration / Employees</p><h1>Add employee</h1><p className="subtitle">The staff member receives a secure, single-use activation link. No password is created or sent by an administrator.</p><EmployeeOnboardingForm locations={locations} departments={departments} managers={managers.map(x=>({...x,name:`${x.firstName} ${x.lastName}`}))}/></main>}
