import { getCurrentUser } from "@/features/auth/session";import { db } from "@/lib/db";export default async function Holidays(){const u=(await getCurrentUser())!;const rows=await db.bankHoliday.findMany({where:{organisationId:u.employee!.organisationId},orderBy:{date:"asc"}});return <main className="page"><h1>Bank holidays</h1>{rows.length?<div className="table-wrap"><table><thead><tr><th>Name</th><th>Date</th><th>Region</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.date.toLocaleDateString("en-GB")}</td><td>{r.locationOrRegion??"All"}</td><td>{r.active?"Active":"Disabled"}</td></tr>)}</tbody></table></div>:<div className="card"><h2>No bank holidays configured</h2><p className="muted">Add the confirmed England &amp; Wales dates before accepting production requests.</p></div>}</main>}
